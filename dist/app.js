"use strict";

let message = "блять как с тайпскриптом";
function add(x, y) {
    return x + y;
}
// Пример использования
console.log(message);
console.log(`2 + 3 = ${add(2, 3)}`);
// Использование интерфейса
const user = {
    name: "глеб",
    age: 14
};
console.log(`Пользователь: ${user.name}, возраст: ${user.age}`);
